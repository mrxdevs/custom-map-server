#Run command map by docker container
'''docker run --rm -it -v $(pwd):/data -p 8080:8080 maptiler/tileserver-gl'''

if it is being run is GCP server then run all docker command by using ''' sudo ''' command
